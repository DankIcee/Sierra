[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT app:app"
healthcheckPath = "/health"
healthcheckTimeout = 100

[phases.setup]
nixPkgs = ["cmake", "gcc", "python39", "python39Packages.pip", "libopenblas", "liblapack", "libx11", "gtk3"]